# Capstone configuration for evaluator
run: docker-compose up -d
seed: docker-compose exec app python scripts/seed.py
test: pytest
base_url: http://localhost:8000

endpoints:
  - /health
  - /api/docs
  - /api/auth/register
  - /api/auth/login
  - /api/widgets
  - /public/submissions